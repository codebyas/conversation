# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BookingAssistant' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for BookingAssistant

	pod 'NoChat', '~> 0.3'
	pod 'YYText'
    	pod 'HPGrowingTextView'

  target 'BookingAssistantTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'BookingAssistantUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
